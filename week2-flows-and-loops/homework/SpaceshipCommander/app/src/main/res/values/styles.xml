<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">
        <!-- Customize your theme here. -->
    </style>

    <style name="GuiButton">
        <item name="android:layout_width">@dimen/gui_button_width</item>
        <item name="android:textSize">@dimen/gui_button_text_size</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
